import { store as auth } from '@/modules/auth'
import { store as series } from '@/views/series'
import { store as watchlist } from '@/views/watchlist'
import { store as showSerie } from '@/views/show-serie'
import { store as watchedlist } from '@/views/watchedlist'

export default {
  auth,
  series,
  watchlist,
  showSerie,
  watchedlist
}
